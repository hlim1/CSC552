/*
    .init        = imp_init,
    .destroy     = imp_destroy,
    .getattr     = imp_file_getattr,
    .fgetattr    = NULL,
    .access      = NULL,
    .readlink    = NULL,
    .readdir     = imp_dir_read,
    .mknod       = NULL,
    .mkdir       = imp_mkdir,
    .symlink     = imp_symlink,
    .unlink      = imp_unlink,
    .rmdir       = imp_rmdir,
    .rename      = imp_rename,
    .link        = imp_link,
    .chmod       = imp_chmod,
    .chown       = imp_chown,
    .truncate    = imp_truncate,
    .ftruncate   = NULL,
    .utimens     = NULL,
    .create      = imp_file_create,
    .open        = imp_file_open,
    .read        = imp_file_read,
    .write       = imp_file_write,
    .statfs      = imp_statfs,
    .release     = imp_file_release,
    .opendir     = imp_dir_open,
    .releasedir  = imp_dir_release,
    .fsync       = NULL,
    .flush       = NULL,
    .fsyncdir    = NULL,
    .lock        = NULL,
    .bmap        = NULL,
    .ioctl       = NULL,
    .poll        = NULL,
#ifdef HAVE_SETXATTR
    .setxattr    = NULL,
    .getxattr    = NULL,
    .listxattr   = NULL,
    .removexattr = NULL,
#endif
    .flag_nullpath_ok = 0,                
};
*/
